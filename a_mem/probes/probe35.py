"""Probe 35: THE IMPOSTER CEILING (bifurcation & malleability).
Score distributions over known/withheld derived words:
  TRUE bindings  mean 0.9970, min 0.9103 (low tail = allomorph physics -> safe refusals)
  best WRONG binding: max 0.9769  <- THE IMPOSTER CEILING
  shipped theta=0.98 sits one notch above it: why zero-confab works.
Apparent unknown-false-accept at 1.0000 was LABEL NOISE: withheld base+ing
coincided with a word known as a bare base ('listing' pattern) -- miner
hygiene: exclude withheld derived forms that are themselves known bases.
Malleability = plateau width between ceiling and true mass (~0.015 at the
loop -- stiff) vs stage plateaus (~0.2 -- soft): hard where it asserts,
malleable where it attends.
Reference: sweep + distribution code in session log; rebuild from probe19
machinery (scores = obs @ bound over known bindings and bare bases).
"""
